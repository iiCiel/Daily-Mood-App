diff --git a/node_modules/@expo/ngrok/src/client.js b/node_modules/@expo/ngrok/src/client.js
index 3f748a3..1ef1cdc 100644
--- a/node_modules/@expo/ngrok/src/client.js
+++ b/node_modules/@expo/ngrok/src/client.js
@@ -6,6 +6,9 @@ class NgrokClientError extends Error {
     this.name = "NgrokClientError";
     this.response = response;
     this.body = body;
+    if (body && typeof body === "object" && body.code) {
+      this.code = body.code;
+    }
   }
 }
 
@@ -28,18 +31,24 @@ class NgrokClient {
       }
     } catch (error) {
       let clientError;
+      const responseBody = error.response && error.response.body;
       try {
-        const response = JSON.parse(error.response.body);
+        const response = JSON.parse(responseBody);
         clientError = new NgrokClientError(
           response.msg,
           error.response,
           response
         );
       } catch (e) {
+        const message =
+          responseBody || error.message || (error && error.toString());
         clientError = new NgrokClientError(
-          error.response.body,
+          message,
           error.response,
-          error.response.body
+          responseBody || {
+            msg: message,
+            code: error.code,
+          }
         );
       }
       throw clientError;
@@ -52,8 +61,28 @@ class NgrokClient {
         (response) => response.statusCode === 204
       );
     } catch (error) {
-      const response = JSON.parse(error.response.body);
-      throw new NgrokClientError(response.msg, error.response, response);
+      const responseBody = error.response && error.response.body;
+      let clientError;
+      try {
+        const response = JSON.parse(responseBody);
+        clientError = new NgrokClientError(
+          response.msg,
+          error.response,
+          response
+        );
+      } catch (e) {
+        const message =
+          responseBody || error.message || (error && error.toString());
+        clientError = new NgrokClientError(
+          message,
+          error.response,
+          responseBody || {
+            msg: message,
+            code: error.code,
+          }
+        );
+      }
+      throw clientError;
     }
   }
 
diff --git a/node_modules/@expo/ngrok/src/utils.js b/node_modules/@expo/ngrok/src/utils.js
index 1c2288a..95e2be0 100644
--- a/node_modules/@expo/ngrok/src/utils.js
+++ b/node_modules/@expo/ngrok/src/utils.js
@@ -34,11 +34,17 @@ function validate(opts) {
 
 function isRetriable(err) {
   if (!err.response) {
-    return false;
+    const code = err.code || (err.body && err.body.code);
+    const message = err.message || (err.body && err.body.msg) || "";
+    return (
+      ["ECONNREFUSED", "ECONNRESET", "ETIMEDOUT"].includes(code) ||
+      /ECONNREFUSED|ECONNRESET|ETIMEDOUT|socket hang up/i.test(message)
+    );
   }
   const statusCode = err.response.statusCode;
-  const body = err.body;
-  const notReady500 = statusCode === 500 && /panic/.test(body);
+  const body = err.body || {};
+  const bodyText = typeof body === "string" ? body : JSON.stringify(body);
+  const notReady500 = statusCode === 500 && /panic/.test(bodyText);
   const notReady502 =
     statusCode === 502 &&
     body.details &&
